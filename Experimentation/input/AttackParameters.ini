[attack]
percentage_vuln_devices = {percentage_vuln_devices}
infection_rate_per_step = {infection_rate_per_step}
patch_rate_per_step = {patch_rate_per_step}
infection_start = {infection_start}
patch_start = {patch_start}
attack_behaviour = {attack_behaviour}
patch_stop = 60000 # is not used
infection_stop = 1000 # is not used